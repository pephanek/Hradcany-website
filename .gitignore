# Python cache
__pycache__/
*.pyc

# Local backups (not part of the published site)
_backups/

# Deploy secrets (token) - never commit
.deploy/

# OS junk
.DS_Store
Thumbs.db

# Generator: seznam nepřeložených řetězců (pracovní soubor)
i18n/chybejici.txt

# Lokalni skripty pro publikaci z tohoto pocitace (nepatri na web)
publikuj.bat
publikuj-skryte.vbs
publikuj.log
publikuj.log.old

# Scratch/QA files (not part of the published site; large binaries here
# were repeatedly breaking pushes over GitHub's 100 MB limit)
_to_delete/
*.tar.gz
*.zip
